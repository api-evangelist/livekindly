generated: '2026-08-04'
method: generated
source: >-
  Generated by the API Evangelist enrichment pipeline from openapi/livekindly-content-openapi.yml,
  mcp/livekindly-mcp.yml and the conventions/errors artifacts in this repo. LIVEKINDLY publishes
  no AGENTS.md or skill files of its own (probed 2026-08-04) — these are API Evangelist's, not the
  provider's.
note: >-
  Every operationId referenced in these skills was verified present in
  openapi/livekindly-content-openapi.yml. Nothing is invented, and no skill promises a capability
  the public surface does not have.
skills:
- file: livekindly-read-newsroom.md
  name: Read the LIVEKINDLY newsroom
  api: openapi/livekindly-content-openapi.yml
  operations: [getPosts, getPostsById, getCategories, getCategoriesById, getMediaById]
  summary: >-
    Page through the 39 corporate news releases and LiveKindly Blog articles with correct
    pagination, sparse fieldsets and content-type-safe error handling.
- file: livekindly-brands-and-partners.md
  name: Enumerate LIVEKINDLY brands, partners and open roles
  api: openapi/livekindly-content-openapi.yml
  operations: [getBrand, getBrandById, getPartner, getPartnerById, getJob, getJobById, getMedia, getSearch]
  summary: >-
    Read the brand (4), partner (4) and job (6) directories — and understand that these custom
    post types return a name, a slug and a link, and nothing else.
- file: livekindly-connect-mcp.md
  name: Connect to the LIVEKINDLY MCP server
  api: mcp/livekindly-mcp.yml
  operations: [initialize, tools/list]
  summary: >-
    Follow the RFC 9728 bearer challenge and the RFC 8414 authorization-server metadata to the
    OAuth authorization-code + PKCE flow guarding the WordPress MCP Adapter endpoint — and stop
    short of asserting a tool set that is not public.
